/**
 * mqScoreboard blocks
 */
namespace mqlib {
    let leftName = '';
    let rightName = '';
    let leftScore = 0;
    let rightScore = 0;

    //% subcategory="计分板"
    //% block="初始化计分板 左边名字 右边名字"
    //% v.defl=1
    export function initScoreboard(v: number) {
        
    }
}